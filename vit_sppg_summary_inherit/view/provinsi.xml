<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- search -->
        <!--record id="view_provinsi_search" model="ir.ui.view">
            <field name="name">provinsi</field>
            <field name="model">vit.master_provinsi</field>
            <field name="inherit_id" ref=""/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- action window -->
        <!--record id="vit_sppg_summary.action_provinsi" model="ir.actions.act_window">
            <field name="name">Provinsi</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.master_provinsi</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Provinsi
                </p><p>

                </p>
            </field>
        </record-->

        <menuitem id="menu_provinsi" name="Provinsi" parent="vit_sppg_summary_config_menu" action="action_provinsi" sequence="20"/>
    </data>
</odoo>